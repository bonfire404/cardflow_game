# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['python_prototype\\ui\\main.py'],
    pathex=['python_prototype'],
    binaries=[],
    datas=[('assets', 'assets'), ('python_prototype', 'python_prototype')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=1,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='Cardflow',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='assets/images/cardflow_logo.ico',
)
